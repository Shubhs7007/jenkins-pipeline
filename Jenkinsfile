pipeline {
   agent any {
	    environment {
		    NAME = "shubham"
			PROF ="devops" 
			AGE= "23"
			}
		}
		
	stages {
       stage("dir") {
               steps {
             echo "hello world"
	     echo "$NAME"
		echo "$PROF
                 echo "$AGE"
				}
			}
		}
}
