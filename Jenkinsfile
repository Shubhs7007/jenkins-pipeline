pipeline {
   agent any {
	    environment {
		    NAME = "shubham"
			ROLE ="devops" 
			AGE= "23"
			}
		}
		
	stages {
       stage("dir") {
               steps {
             echo "hello world"
	     echo "$NAME"
		echo "$ROLE
                 echo "$AGE"
				}
			}
		}
}
